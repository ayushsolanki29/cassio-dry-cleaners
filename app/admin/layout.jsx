export const metadata = {
  title: "Admin Dashboard | Cassio Dry Cleaners",
  description: "Admin management portal for Cassio Dry Cleaners",
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
